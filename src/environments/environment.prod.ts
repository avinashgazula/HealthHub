export const environment = {
  production: true,
  // add firebase config key and google maps api key
  // firebaseConfig: {  },
  // googleMapsApiKey: "",
  serverUrl: 'https://health-hub-5709-server.herokuapp.com',
  passwordRegex: '(?=.*\\d)(?=.*[a-z])(?=.*[A-Z]).{8,}',
};
